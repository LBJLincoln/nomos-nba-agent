{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qiE 'sklearn\\..*fit|xgboost.*train|lightgbm.*train|catboost.*fit|torch\\..*backward|tensorflow.*fit|model\\.fit|model\\.train'; then echo 'BLOCKED: No ML training on VM (1 vCPU / 969MB RAM). Use HF Spaces or Colab.' >&2; exit 1; fi"
          }
        ]
      }
    ]
  },
  "model": "claude-opus-4-6"
}
